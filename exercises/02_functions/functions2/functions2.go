package functions2

// I AM NOT DONE

// Implement DivMod so it returns (quotient, remainder).
// Don't change the signature — just fill in the body.

func DivMod(a, b int) (int, int) {
	return 0, 0
}
